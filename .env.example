# Frontend Environment Variables

# Backend API URL (required for production deployments like Vercel)
# In development, this can be left empty as the proxy will handle it
# Example: https://your-backend-domain.com or https://your-replit-app.replit.dev
VITE_API_BASE_URL=

# Socket.IO Backend URL (optional, defaults to current origin)
# Example: https://your-backend-domain.com
VITE_SOCKET_BASE_URL=
